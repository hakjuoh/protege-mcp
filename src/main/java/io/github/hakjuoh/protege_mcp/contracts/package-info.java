/**
 * Versioned, surface-neutral ontology-engineering contracts shared by future policy, change-set,
 * validation and release services. JSON uses the snake_case names declared on each record component.
 * Consumers should validate the packaged schema and use {@link io.github.hakjuoh.protege_mcp.contracts.ContractJson}
 * so unknown and duplicate JSON fields fail closed.
 */
package io.github.hakjuoh.protege_mcp.contracts;
