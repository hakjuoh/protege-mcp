# Optional: local preview of the docs site.
#
#   cd docs
#   bundle install
#   bundle exec jekyll serve   # → http://127.0.0.1:4000/protege-mcp/
#
# GitHub Pages itself does not use this file — it builds the site with the
# `github-pages` gem set. This is only for previewing changes locally.
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "just-the-docs", "0.12.0"
gem "jekyll-remote-theme"
gem "jekyll-seo-tag"
